using System.IO;
using System.Xml;
using DataLibrary;
using Microsoft.Xna.Framework;

namespace DataBuilder
{
    class Program
    {
        static void Main(string[] args)
        {
            //Animations animations = new Animations();
            //animations.AnimationsList = new Animation[1];
            //animations.AnimationsList[0] = new Animation();
            //animations.AnimationsList[0].FramePositions = new Point[2];
            //animations.AnimationsList[0].FramePositions[0] = new Point();
            //animations.AnimationsList[0].FramePositions[1] = new Point();

            //XmlWriterSettings settings = new XmlWriterSettings();
            //settings.Indent = true;

            //var outputPath = Directory.GetParent(Directory.GetCurrentDirectory()).Parent.FullName + Path.DirectorySeparatorChar + "Output";

            //using (XmlWriter writer = XmlWriter.Create(outputPath + Path.DirectorySeparatorChar + "newAnimations.xml", settings))
            //{
            //    Microsoft.Xna.Framework.Content.Pipeline.Serialization.Intermediate.
            //    IntermediateSerializer.Serialize(writer, animations, null);
            //}
        }
    }
}
